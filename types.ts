
export interface Article {
  id: string;
  title: string;
  category: string;
  readTime: string;
  date: string;
  imageUrl: string;
  categoryColor: string;
}

export interface Category {
  id: string;
  name: string;
  imageUrl: string;
}
